name: My First workflow

on:
  push:

jobs:
  Job-1:
    runs-on: ubuntu-latest

    steps:
      - name: Print a message
        run: echo "Git hub action Practice"

      - name: Print the date
        run: date

      - name: List files
        run: ls -la
